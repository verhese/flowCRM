changeLog=classpath:db/changelog/db.changelog-master.yaml

# Default test database (H2 in-memory) for unit tests and CI
url=jdbc:h2:mem:flowcrm;DB_CLOSE_DELAY=-1;MODE=MYSQL
username=sa
password=
driver=org.h2.Driver

# Alternate MySQL database (development/local)
# Uncomment when using MySQL and ensure the schema exists: flowCRM
# url=jdbc:mysql://localhost:3306/flowCRM?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# username=root
# password=secret
# driver=com.mysql.cj.jdbc.Driver

classpath=target/test-classes;target/classes
