package wfm.model.simple;

import wfm.model.AbstractService;

public final class SimpleService extends AbstractService {
    /**
     * Standard constructor for creating a service implementation.
     *
     * @param code The unique code which is used to identify the service.
     */
    public SimpleService(String code) {
        super(code);
    }
}
